import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#008acf',
      light: '#97d6f8',
      dark: '#072945'
    },
    secondary: {
      main: '#000000',
      light: '#f3f3f3',
      dark: '#4b4b4b',
    },
  },
});

export default theme;